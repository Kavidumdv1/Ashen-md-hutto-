// lib/cmd.js

function cmd(config, callback) {
  // ඔබේ bot command handler එකට connect කරන්න ඕනේ
  console.log(`✅ Command registered: ${config.pattern}`);
  // මේක simple placeholder එක, real handler එකට adapt කරන්න
}

module.exports = cmd;
